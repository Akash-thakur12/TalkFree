# Copy to .env next to index.js and fill in real values.
#
# -----------------------------------------------------------------------------
# Render (talkfree-server) — quick map (see also index.js requireEnv + defaults)
# -----------------------------------------------------------------------------
#
# REQUIRED AT BOOT — empty/missing → server exits:
#   TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_CALLER_ID,
#   TWILIO_API_KEY_SID, TWILIO_API_KEY_SECRET,
#   and exactly ONE TwiML App SID: use TWILIO_TWIML_APP_SID (preferred) OR TWILIO_APP_SID.
#   If both are set to the same AP… value on Render, delete one — redundant and confusing.
#
# REQUIRED FOR WEBHOOKS — no trailing slash:
#   PUBLIC_BASE_URL (e.g. https://talkfree-server.onrender.com)
#
# REQUIRED IN PRACTICE FOR THE APP — unset → Firebase Bearer routes return 503:
#   FIREBASE_SERVICE_ACCOUNT_JSON (single-line service account JSON)
#
# RENDER SETS PORT — you usually omit PORT on Render; keep for local only.
#
# ADD ON RENDER WHEN USING PRO CHECKOUT (Razorpay):
#   RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET, and optionally RAZORPAY_CURRENCY (default INR).
#   Flutter only needs RAZORPAY_KEY_ID in project-root .env — never put the secret in the app.
#
# OPTIONAL HTTP CRON (Render Cron Job → POST /jobs/run-number-janitor):
#   CRON_SECRET (sent as X-Cron-Secret)
#
# OPTIONAL TUNING — defaults are in index.js if unset (PLAN_*_CREDITS, REWARD_GRANT_*, etc.).
#
# ADMIN_SECRET_KEY — only if you call POST /admin/upgrade-user (or similar admin flows).
#
# Billing integrity (call-live-tick, settlement) and Firestore field cleanup do not need
# extra env keys beyond correct Twilio + Firebase above.
#
# Default premium/plan numbers match Flutter [lib/config/credits_policy.dart] — change env only
# together with app copy/constants if you want a different product: CALL_CREDITS_PER_MINUTE_PREMIUM=7,
# REWARD_GRANT_CREDITS_PREMIUM=20, MAX_ADS_PER_DAY_PREMIUM=25, AD_GAP_SECONDS_PREMIUM=10,
# PREMIUM_MONTHLY_BONUS_CREDITS=400, PREMIUM_WELCOME_BONUS=500, STARTER_PACK_CREDITS=135,
# PLAN_DAILY_CREDITS=50, PLAN_WEEKLY_CREDITS=200, PLAN_MONTHLY_CREDITS=700, PLAN_YEARLY_CREDITS=5000.
# -----------------------------------------------------------------------------

# Public URL of this server (Twilio voice + SMS webhooks). No trailing slash — otherwise
# URLs become https://host//sms-webhook and Twilio can reject or mis-route requests.
# Render example: https://your-service.onrender.com
PUBLIC_BASE_URL=https://talkfree-server.onrender.com

# Twilio wallet: each US local number is ~US$1.15/mo; concurrent "Claim" = one purchase per user.
# Keep a few dollars in the account if multiple users may provision at once.

# REST API (outbound calls via GET /call or POST /call with JSON, Twilio client)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
# E.164 caller ID for Voice + fallback `From` for POST /send-sms when user has no assigned_number.
TWILIO_CALLER_ID=+1xxxxxxxxxx

# Flutter Voice SDK — GET /token (JWT)
TWILIO_API_KEY_SID=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_API_KEY_SECRET=your_api_key_secret
# Use ONE of the next two (same TwiML App SID). index.js uses TWILIO_TWIML_APP_SID || TWILIO_APP_SID.
TWILIO_TWIML_APP_SID=APxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_APP_SID=APxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

PORT=3000

# Razorpay (TalkFree Pro) — Orders API + POST /verify-payment (HMAC). Key secret NEVER in the Flutter app.
# Uncomment and set on Render when Pro payments are live.
# RAZORPAY_KEY_ID=rzp_test_xxxxx
# RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxx
# RAZORPAY_CURRENCY=INR

# Browse-number flow: POST /purchase-browse-number (must match Flutter VirtualNumber.defaultPrice).
# BROWSE_NUMBER_PRICE=500

# Welcome bonus on first Pro activation (POST /verify-payment). Matches server default.
PREMIUM_WELCOME_BONUS=500

# POST /grant-reward — credits per rewarded ad after first lifetime ad. Matches server defaults.
REWARD_GRANT_CREDITS_FREE=10
REWARD_GRANT_CREDITS_PREMIUM=20

# Firebase Admin JSON (single line) — set on Render for production; unset → Bearer routes return 503.
# Enables POST /grant-reward, POST /assign-number, POST /send-sms, POST /admin/upgrade-user, token verification.
# Paste the full service account JSON from Firebase Console → Project settings → Service accounts.
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# POST /admin/upgrade-user — long random string; send as header X-Admin-Secret or Authorization: Bearer …
# (for payment webhooks later). Never commit the real value.
# ADMIN_SECRET_KEY=your-long-random-secret
# Optional override for bonus credits on POST /admin/upgrade-user (default 1000).
# ADMIN_UPGRADE_BONUS_CREDITS=1000

# Optional grant-reward overrides (see index.js): FIRST_AD_GRANT_CREDITS, MAX_ADS_PER_DAY_*,
# AD_GAP_SECONDS_*. Legacy: REWARD_GRANT_CREDITS only applies if REWARD_GRANT_CREDITS_FREE is unset.

# POST /claim-welcome-bonus — disabled in server (returns granted:false; no env).

# Outbound call billing: free tier (default 10) vs premium tier (default 7) — see POST /call-live-tick & settlement.
# CALL_CREDITS_PER_MINUTE=10
# CALL_CREDITS_PER_MINUTE_PREMIUM=7

# POST /assign-number — Twilio search + purchase. Eligible: isPremium OR lifetime ads ≥ ASSIGN_NUMBER_MIN_ADS_WATCHED OR usable credits ≥ ASSIGN_NUMBER_MIN_CREDITS.
# JSON body: { "planType": "daily"|"weekly"|"monthly"|"yearly" } — credits deducted + number_expiry_date set.
# Premium users pay 0 credits but still pick a plan for lease duration.
# Optional overrides (omit on Render to use index.js defaults — not duplicate sources of truth):
# PLAN_DAILY_CREDITS=50
# PLAN_WEEKLY_CREDITS=200
# PLAN_MONTHLY_CREDITS=700
# PLAN_YEARLY_CREDITS=5000
# PLAN_DAILY_MS=86400000
# PLAN_WEEKLY_MS=604800000
# PLAN_MONTHLY_MS=2592000000
# PLAN_YEARLY_MS=31536000000
# ASSIGN_NUMBER_MIN_CREDITS=100
# ASSIGN_NUMBER_MIN_ADS_WATCHED=50
#
# Optional US area code (NPA) for available-number search, e.g. 415. If set, search is limited to
# that area; if unset, Twilio returns numbers from the broader US local inventory (not “one fixed”
# area — pool depends on Twilio availability).
# ASSIGN_NUMBER_AREA_CODE=415
#
# US SMS (A2P / 10DLC): Twilio may require brand/campaign registration before inbound SMS delivers.
# If messages are not received, check Twilio Console → Messaging / Trust Hub / Messaging Policy.
#
# Inbound SMS webhook (must be reachable on the public internet): {PUBLIC_BASE_URL}/sms-webhook
# The server responds 200 with empty TwiML so Twilio knows the webhook succeeded (no error loop).

# Hourly job (default): release Twilio numbers whose numberExpiry / number_expiry_date / expiry_date is in the past.
# Cron is UTC. Set DISABLE_NUMBER_JANITOR=1 to skip (e.g. local dev).
# NUMBER_JANITOR_CRON=0 * * * *
# DISABLE_NUMBER_JANITOR=1
# Optional HTTP cron (Render / cron-job.org): same `CRON_SECRET` in header `X-Cron-Secret` for:
#   POST /jobs/run-number-janitor — Twilio number lease cleanup (default cron 0 * * * * UTC).
#   POST /jobs/run-premium-expiry-janitor — demote users past expiry (`isPremium` boolean true or legacy string "true").
# Canonical URL (replace host): https://talkfree-server.onrender.com/jobs/run-premium-expiry-janitor
# Alias (same POST + header): https://talkfree-server.onrender.com/jobs/premium-expiry-janitor
# CRON_SECRET=your-long-random-secret
# PREMIUM_EXPIRY_JANITOR_CRON=15 * * * *
# DISABLE_PREMIUM_EXPIRY_JANITOR=1

# Free-tier line lease (ms) for POST /assign-free-number, assign-number (non‑Pro), purchase-browse-number (default 24h).
# FREE_TIER_NUMBER_LEASE_MS=86400000

# POST /renew-number — watch N rewarded ads (near expiry) or spend credits to extend expiry_date.
# NUMBER_RENEW_ADS_REQUIRED=5
# NUMBER_RENEW_CREDITS=100
# Max successful renews per UTC calendar day per user (default 2).
# MAX_RENEWALS_PER_DAY=2

# After janitor releases a Twilio number, block re-provisioning that E.164 for this many ms (default 24h).
# NUMBER_REUSE_COOLDOWN_MS=86400000

# Pro `premium` tier lines: extra delay after subscription expiry before janitor releases (default 6h).
# PREMIUM_SUBSCRIPTION_GRACE_MS=21600000

# POST /send-sms — credits deducted per outbound SMS (incoming SMS remains free on Twilio).
# SMS_OUTBOUND_CREDIT_COST=3

# Max Twilio lines per user (Pro can claim a second line via POST /purchase-number when primary exists).
# MAX_TWILIO_LINES_FREE=1
# MAX_TWILIO_LINES_PREMIUM=2
